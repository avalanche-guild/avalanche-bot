version: '3'

services:
  bot:
    image: docker.com/avalanche-guild/avalanche-bot
    build:
      context: ./
    environment:
      DISCORD_TOKEN: "${DISCORD_TOKEN}"
    tty: true
    volumes:
      - /data/avalanche-bot/stats/:/bot/stats/
